import { Tensor, TensorsObject } from "./tensor";
type RunOptions = {
    stream: boolean;
    postProcessing?: Function;
};
export type SessionOptions = {
    extraHeaders?: {};
    ctx?: any;
    debug?: boolean;
    apiEndpoint?: string;
};
export declare class InferenceSession {
    private binding;
    private model;
    private options;
    constructor(binding: any, model: string, options?: SessionOptions);
    run(inputs: Array<Tensor<any>> | TensorsObject, options?: RunOptions): Promise<TensorsObject | ReadableStream>;
}
export {};
//# sourceMappingURL=session.d.ts.map